import { defineSchema } from "convex/server";

/**
 * Schema inicial — [S0-3].
 * As tabelas de domínio serão adicionadas issue a issue conforme SPRINTS.md:
 * S1 (users, consents, students), S3 (jobs), S4 (applications),
 * S7 (extensionProjects). Referência: CEREBRO.md §4.1.
 */
export default defineSchema({});
